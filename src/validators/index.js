const validateAuth = require('./auth');
const validateLibs = require('./libs');
const validateTimeframe = require('./timeframe');

module.exports = {
  validateAuth,
  validateLibs,
  validateTimeframe,
};
